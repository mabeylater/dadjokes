type Favorites
  @model
  @auth(
    rules: [
      { allow: owner, operations: [read, update] }
      { allow: private, operations: [create] }
    ]
  ) {
  owner: String
  jokeIds: [String]
  catFacts: [String]
}
